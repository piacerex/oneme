# oneme ROADMAP

Ready Player Me のような「Webで作成でき、複数アプリから利用できる3Dアバター生成サービス」を目指す。
最初から完全互換を狙わず、まずはパーツ選択式の軽量MVPを作り、早い段階で顔写真からの初期アバター提案を追加する。
高度なAI生成、SDK、VRM対応は後続フェーズで拡張する。

## 目標

- ユーザーがWeb上で3Dアバターを作成できる
- 作成したアバターをアカウントに保存できる
- 外部アプリがAPI経由でアバター情報やモデルURLを取得できる
- `.glb` を基本形式として配信し、必要に応じて `.vrm` に対応する
- Unity / Web / Three.js から簡単に読み込める

## 想定ユースケース

- メタバース、VR/AR、Web3Dサービス向けの共通アバター
- UnityゲームやWebアプリに組み込めるユーザーアバター
- 社内イベント、バーチャル展示会、ライブ配信向けの簡易アバター
- 顔写真や画像生成AIと連携した似顔アバター生成

## 方針

- Phase 1では手動作成を成立させ、Phase 2で「顔写真から初期アバター案を作る」体験を前倒しする
- Phase 2の顔写真対応は完全自動生成ではなく、肌色、髪色、顔パーツの推薦から始める
- 最初は高品質な少数アセットで、確実に動く作成体験を優先する
- サーバー側で巨大な3D処理を抱えすぎず、まずは設定JSON + パーツURLで成立させる
- 合成済みモデル出力は後続フェーズで追加する
- API、埋め込みWidget、SDKの順に外部連携を広げる

## 推奨技術スタック

- Frontend: React / Next.js / Three.js / React Three Fiber
- Backend: Phoenix / Node.js / Hono など
- Database: PostgreSQL
- Storage: S3互換ストレージ
- CDN: Cloudflare / CloudFront
- 3D format: `.glb` first, `.vrm` later
- 3D tooling: Blender Python, gltf-transform, meshoptimizer
- Auth: Email login, OAuth
- API: REST first, GraphQL optional

## Phase 0: 企画とプロトタイプ準備

- サービス名、ドメイン、ブランド方向性を決める
- アバターの表現テイストを決める
  - リアル寄り
  - アニメ寄り
  - デフォルメ寄り
  - ビジネス向け
- MVPで扱うアバター構成を決める
  - ベースボディ
  - 肌色
  - 髪型
  - 顔パーツ
  - トップス
  - ボトムス
  - 靴
  - アクセサリ
- ライセンス上問題なく使える3Dアセット調達方針を決める
- `.glb` の読み込み、表示、パーツ差し替え検証を行う

## Phase 1: MVP

### 機能

- 3Dアバター作成画面
- Three.jsによるリアルタイムプレビュー
- パーツ選択UI
- 肌色、髪型、服、アクセサリの選択
- 選択内容の保存
- アバター詳細画面
- 公開アバターURL
- APIでアバター設定JSONを取得

### データモデル

- users
- avatars
- avatar_parts
- avatar_part_categories
- avatar_selections
- asset_files

### API

- `POST /api/avatars`
- `GET /api/avatars/:id`
- `PATCH /api/avatars/:id`
- `GET /api/avatars/:id/config`
- `GET /api/parts`

### 完了条件

- ユーザーがブラウザでアバターを作成できる
- 作成したアバターを再表示できる
- 外部アプリが設定JSONを取得できる
- 3DプレビューがPCとスマホで破綻しない

## Phase 2: 顔写真から初期アバター提案

### 機能

- 顔写真アップロード
- 顔写真の一時保存と自動削除
- 肌色の推定
- 髪色の推定
- 顔の輪郭や雰囲気に近い顔パーツの推薦
- 推薦結果から初期アバターを生成
- ユーザーが手動で補正できるUI
- 顔写真を使わずに作成する導線

### 方針

- 完全な3D顔復元は行わない
- 写真から直接メッシュを生成せず、既存パーツの中から近いものを選ぶ
- 似顔精度よりも「すぐ始められる」「あとで直せる」体験を優先する
- 顔写真は初期提案にだけ使い、保存し続けない

### データモデル

- face_analysis_jobs
- face_analysis_results
- avatar_recommendations

### API

- `POST /api/face_analysis_jobs`
- `GET /api/face_analysis_jobs/:id`
- `POST /api/avatars/from_face_analysis`

### 注意点

- 顔写真の保存ポリシーを明確にする
- 削除機能を必ず用意する
- 未成年やセンシティブ情報の扱いに注意する
- 顔写真を使わない作成方法を常に用意する

### 完了条件

- 顔写真から初期アバター案を生成できる
- ユーザーが生成結果を編集して保存できる
- 顔写真が一定時間後に自動削除される

## Phase 3: GLBエクスポート

### 機能

- 選択済みパーツを1つの `.glb` として出力
- エクスポートジョブ管理
- 生成済みモデルのキャッシュ
- モデル軽量化
- テクスチャ最適化

### 3D処理

- Blender Pythonでパーツ合成
- gltf-transformで最適化
- Dracoまたはmeshoptimizerによる圧縮
- LODまたは低ポリ版の生成

### 完了条件

- APIから合成済み `.glb` URLを取得できる
- Unity / Three.js で読み込める
- 生成処理が非同期ジョブとして安定して動く

## Phase 4: 埋め込みWidget

### 機能

- 外部サービスに埋め込めるiframe版アバター作成Widget
- 作成完了時のcallback
- アプリごとのテーマ設定
- アプリごとの許可パーツ制御
- API key管理

### API

- `POST /api/apps`
- `POST /api/apps/:id/api_keys`
- `GET /widget?app_id=...`
- `postMessage` による作成完了通知

### 完了条件

- 外部HTMLに数行でアバター作成画面を埋め込める
- 作成完了後にavatar idを呼び出し元へ返せる

## Phase 5: Unity / Web SDK

### Unity SDK

- avatar idから `.glb` を読み込む
- ランタイムでモデルを表示する
- Animator連携のサンプルを用意する
- Addressablesやキャッシュに対応する

### Web SDK

- npm packageとして提供
- avatar idから設定JSONまたはモデルURLを取得
- Three.jsで表示するサンプルを用意する

### 完了条件

- UnityプロジェクトにSDKを追加してアバターを表示できる
- Webアプリから数行でアバターを表示できる

## Phase 6: VRM対応

### 機能

- `.vrm` エクスポート
- humanoid rig対応
- 表情プリセット
- 揺れ物設定
- VRMビューアでの検証

### 完了条件

- VRM対応アプリで読み込める
- 基本的な表情とアニメーションが動く

## Phase 7: 高度なAI生成

### 機能

- 顔特徴の詳細推定
- 髪型、顔パーツ、メイク、服装の高度な自動提案
- 画像生成AIによるテクスチャ生成
- 2D写真から3Dテクスチャ候補を生成
- 複数スタイルのアバター案を同時生成
- ユーザーの編集履歴を使った推薦改善

### 注意点

- 顔写真の保存ポリシーを明確にする
- 削除機能を必ず用意する
- 未成年やセンシティブ情報の扱いに注意する
- 生成結果が本人に似すぎる場合の利用規約を整える

### 完了条件

- 複数の高品質なアバター案を生成できる
- 画像生成AIによるテクスチャ候補を安全に適用できる
- ユーザーが生成結果を編集して保存できる

## Phase 8: 商用運用

### 機能

- チーム管理
- アプリ別課金
- 使用量計測
- レート制限
- Webhook
- 監査ログ
- アセット審査フロー
- 管理画面

### 運用

- CDNキャッシュ戦略
- 生成ジョブの監視
- モデル生成失敗時の再試行
- アセット破損検知
- API障害時のステータスページ

### 完了条件

- 複数アプリが本番利用できる
- 利用量、エラー、生成ジョブを監視できる
- アセット追加と審査を運用できる

## 初期ディレクトリ案

```text
oneme/
  apps/
    web/
    api/
  packages/
    avatar-viewer/
    sdk-web/
    sdk-unity/
  assets/
    avatars/
    parts/
    textures/
  tools/
    blender/
    gltf/
  docs/
  ROADMAP.md
```

## 最初に作るべき画面

- アバター作成画面
- アバター一覧画面
- アバター詳細画面
- パーツ管理画面
- API key管理画面

## 最初に作るべきAPI

- アバターCRUD
- パーツ一覧
- アバター設定JSON取得
- モデルURL取得
- アセットアップロード

## 最初のマイルストーン

1. Three.jsでベースアバターを表示する
2. 髪型と服を切り替える
3. 選択内容をDBに保存する
4. avatar idから同じ見た目を復元する
5. 顔写真から肌色、髪色、顔パーツを推薦する
6. 推薦結果から初期アバター案を作る
7. 外部ページから設定JSONを取得して表示する
8. `.glb` エクスポートを非同期で実行する

## リスク

- 3Dアセット制作コストが高い
- パーツ同士の干渉や破綻が起きやすい
- リギングとアニメーション互換が難しい
- 顔写真やAI生成はプライバシー対応が必要
- `.glb` と `.vrm` の両対応は検証コストが高い
- 商用アセットのライセンス管理を怠ると後で使えなくなる

## MVPで割り切ること

- 完全な顔写真生成は後回し
- Phase 2では顔写真から既存パーツを推薦するだけにする
- パーツ数は少なくてよい
- 最初は `.glb` のみ
- SDKは後回しで、まずはREST APIとiframe
- サーバー合成前に、設定JSON方式で動かす

## 次のアクション

- 技術スタックを確定する
- 最初の3Dアセットを決める
- Webプレビューの最小実装を作る
- アバター設定JSONのスキーマを定義する
- APIとDBの最小構成を実装する
